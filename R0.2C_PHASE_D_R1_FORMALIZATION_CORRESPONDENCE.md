# R0.2c Phase D-R1 Formalization Correspondence

Status: ACTIVE PHASE D-R1 CORRESPONDENCE LEDGER
Branch: `r0-2c-phase-d-r1-lean`
Immutable Phase C base: `6f82dc88217dd7b5ebe726c129603eea99bab85e`
Controlling theorem source: `R0.2C_THEOREM_SOURCE_RESTORATION_R1`, package SHA-256 `d5a4bc8ee40c075313e55b0d465a4ea2622f6cbd143494298bc5e56cc4c733a4`.

This ledger binds R1 successor theorem statements to Phase D Lean declarations. The lost Phase A theorem-source bytes are not reconstructed. Promotion is permitted only for rows classified `EXACT` or `SEMANTICALLY EQUIVALENT`; semantic-equivalence rows identify an explicit Lean equivalence bridge.

## Proof-layer full semantics

| R1 / frozen object | Lean binding | Status | Evidence / limitation |
|---|---|---|---|
| Trusted state remains the seven-component Phase C `State` | `EffectKernel.State` | EXACT | Phase D adds no field to `State`. Frozen Phase C source hashes are checked on every run. |
| In-epoch transition kinds remain the frozen Phase C set | `StepLabel.kind`, `BaseStep`, `FullStep` | SEMANTICALLY EQUIVALENT | `StepLabel` is proof-only parameterization; `baseStep_to_trusted` and `fullStep_to_trusted` prove projection to the immutable `TrustedStep`. No new transition kind is introduced. |
| Full admission guards omitted from the Phase C foundational projection | `FullGuard`, `PrepareAdmissible`, `AuthorizedAt` | SEMANTICALLY EQUIVALENT | Guards are proof-only predicates over predecessor state/environment. They do not alter Phase C transition constructors. |
| Immutable authorization snapshot | `AuthSnapshot`, `snapshotOf`, `AuthorizedFromSnapshot` | SEMANTICALLY EQUIVALENT | Snapshot contains the actual predecessor `State`, trusted time argument, and effect key; it is proof/history material, not trusted runtime state. `authorizedAt_snapshot_iff` is the explicit equivalence lemma. |
| Arbitrary-length in-epoch full trace | `FullTrace` | SEMANTICALLY EQUIVALENT | `FullTrace.toReachable` projects every full trace to arbitrary-length Phase C `ReachableFrom`. |
| Trace-indexed step occurrence | `StepAt` | EXACT | Proof-only observation relation over an existing `FullTrace`; no runtime state or certificate. |
| Protected release is the release-bearing `COMMIT_START` | `ProtectedRelease.commitStart` | EXACT | No PREPARE or other frozen transition constructor is classified as release-bearing. |

## T3 — Positive Authority Origin

R1 controlling claim: for an in-epoch full step, a grant absent in the predecessor and present in the successor can arise only as the fresh child of the frozen delegation relation, exposing predecessor parent, attenuation guards, and transfer budget; cross-epoch root provisioning is excluded.

Lean: `EffectKernel.PhaseD.T3_positiveAuthorityOrigin`

Correspondence: **EXACT**.

The proof performs case analysis over the actual `BaseStep` relation. All non-delegation cases derive impossibility from frozen gamma-preservation/update equations. The delegation case exposes `Delegate`, predecessor parent existence, parent link, permission attenuation, temporal/delegability attenuation, and `DelegateBudget`. Positive origin is not a constructor premise.

Nonclaims preserved: no cross-epoch root-parent claim; no E2+ authorization theorem; no physical-consequence theorem.

## T5 — Reservation Precedes Release

R1 controlling claim: for a fresh effect key, every protected release has an earlier PREPARE that durably establishes the RESERVED record and reservation vector, and release is the later COMMIT_START for that same binding.

Lean: `EffectKernel.PhaseD.T5_reservationPrecedesRelease`

Correspondence: **EXACT**.

`reserved_successor_decompose` proves from actual transition cases that a RESERVED successor record either was preserved or is created by PREPARE. `reserved_has_prepare` lifts this over an arbitrary `FullTrace`. The final theorem uses the independent `ProtectedRelease` classification and the actual COMMIT_START source/successor equations. No release fact is carried as transition certificate data.

Nonclaims preserved: no concrete storage durability; no deployment complete mediation; no claim for a trace origin that is already reserved.

## T6 — Unique Authorization Linearization

R1 controlling claim: for an initially fresh key, COMMIT_START occurs at at most one trace index; if present, its successor is DISPATCHING with `linearized=true`, and no later in-epoch step can create another COMMIT_START for the same key.

Lean: `EffectKernel.PhaseD.T6_uniqueAuthorizationLinearization`

Correspondence: **EXACT**.

The proof derives uniqueness from one-way Phase C lifecycle reachability. After any COMMIT_START, the key is DISPATCHING; the post-linearization lifecycle set cannot reach RESERVED, while any second COMMIT_START requires RESERVED as its predecessor. Uniqueness is not a transition premise.

Nonclaims preserved: no wall-clock uniqueness, physical exactly-once guarantee, or uniqueness across distinct effect keys.

## T7 — Authorization Snapshot Sufficiency

R1 controlling claim: at the unique authorization linearization, `AuthorizedAt(K_ell,k) <-> AuthorizedFromSnapshot(sigma_k)`; the immutable snapshot remains sufficient without consulting later mutable grant/policy state, and later lifecycle evolution preserves the snapshot binding projection.

Lean: `EffectKernel.PhaseD.T7_authorizationSnapshotSufficiency`

Correspondence: **SEMANTICALLY EQUIVALENT**.

Explicit equivalence bridge: `EffectKernel.PhaseD.authorizedAt_snapshot_iff` proves the R1 authorization judgment equivalence definitionally from the actual predecessor state captured by `snapshotOf`.

The R1 phrase “preserves `sigma_k`” is realized in two separate proof objects rather than by adding snapshot bytes to trusted `State`: (1) `AuthSnapshot` itself is immutable proof/history material containing the predecessor state; (2) `SnapshotBinding` is the Phase C-required `SameEffectBinding` projection, proved at the COMMIT_START successor and at both endpoints of every strictly later full step. The theorem also invokes T6 to re-establish the unique linearization index. This is the narrow interpretation required by the R1 compatibility audit; it does not claim a concrete serializer exists.

Nonclaims preserved: no concrete snapshot serialization correctness, deployment projection adequacy, or restriction on later admissions for other effects.

## T10 — Uncertain-Outcome Conservatism

R1 controlling claim: a valid `COMMIT_FAULT_UNKNOWN` moves the effect from `DISPATCHING` to `UNKNOWN`, moves the complete outstanding reservation into consumed quantity without refund, does not increase lineage mass, and later in-epoch evolution for that key can only remain `UNKNOWN` or reconcile to `COMMITTED`/`ABORTED` rather than return to a fresh pre-dispatch state.

Lean: `EffectKernel.PhaseD.T10_uncertainOutcomeConservatism`

Correspondence: **EXACT**.

The proof reads the exact Phase C `CommitFaultUnknown` and `BurnOutstanding` equations, derives the later-state exclusion from the actual `LifecycleReach` graph through T9 semantics, and appends the fault step to a genuine `BudgetHistory` before invoking T4. The theorem carries no uncertainty-conservatism certificate and does not interpret `UNKNOWN` as evidence that no physical effect occurred.

Failed-attempt preservation: commit `3ee6ad245287eb80578237068f9290fd980b6570`, run `35171883215`, artifact `10476778054` (`sha256:581bbee2e951df37648bf0241c7ae41f6b52bb2b06e8d6726ab8aa9e031d6620`) failed only on a Lean expected-type projection elaboration; classified `FORMALIZATION / TRANSCRIPTION DEFECT`. The theorem statement was unchanged by the repair.

Successful source replay: commit `6153903dc0dae77f78bd247165de19e019bf4a01`, run `35172031478`, artifact `10477191551` (`sha256:c21044312ff11fc6e6c3aa57d28a25f36d25b64a140544ae2e5979ff44c57f77`). Axiom-audit replay: commit `d2572339c4ac3b849d6c9495df479fb55cafc11b`, run `35172098364`, artifact `10477067192` (`sha256:b0f37d09e55a79bb0f50cc3dd1ff4f42c738a7cd4e2557acaaf8a8296391f5bf`). `#print axioms` reports only `propext`, `Classical.choice`, and `Quot.sound`; no project axiom.

Nonclaims preserved: no claim that the physical effect did or did not occur, no universal exactly-once external execution, no deployment anti-rollback claim.

## T11 — Replay Exclusion and Non-Resurrection

R1 controlling claim: after the first successful PREPARE of an initially unused effect key, the durable `used` bit remains true throughout in-epoch continuations, the same key cannot satisfy fresh PREPARE again, lifecycle evolution cannot create a second authorization linearization for that key, replay cannot recreate budget, and consumed meta-authorization cannot be reused for a second positive expansion.

Lean: `EffectKernel.PhaseD.T11_replayExclusionNonResurrection`

Correspondence: **EXACT**.

The proof derives effect-key freshness exclusion from the actual `PrepareLifecycle.preUnused`/`postUsed` equations plus T9 used-bit monotonicity. It independently derives second-COMMIT_START exclusion from the one-way lifecycle graph, and derives meta-authorization non-reuse from `FullStep.metaMonotone` together with the actual `ConsumeMetaAuth.fresh` premise of `UpdatePolicy`. The T4 conjunct applies conservation to any valid budget history ending in the continuation state. No replay flag, non-resurrection certificate, T6, or T23 is used. The theorem is explicitly in-epoch; no deployment anti-rollback property is inferred, and E3 successor behavior is not strengthened beyond the R1 epoch boundary.

Failed-attempt preservation:
- `b7ac88e7697f5cdfb34866154f7cb999ed3564de`, run `35172265395`, artifact `10477127157` (`sha256:eb00843d942a24f55a041642eb8aecce2acf279ef24ae2c1793e958a9983d762`): `TOOLCHAIN DEFECT`, lexical hole scanner matched the English word `admit` in a comment.
- `4f44f84b97a61497f22877b19cb90730bcc3c80c`, run `35172361719`, artifact `10477311132` (`sha256:8972be56beb11240d0e0c0954479c2e54e56380040acefbb72abfbb85bafda02`): `MISSING LEMMA`, a T10-local lifecycle helper was not imported; repaired by deriving the T9 helper locally so the R1 direct-dependency graph remains exact.
- `b38b5a3831f9959e5a43898cf00f200192dd8275`, run `35172473148`, artifact `10476639475` (`sha256:f9a8d9bc775bcaea75d92c1bd4cde660132c150a5f305b17d07516da1214bb8c`): `FORMALIZATION / TRANSCRIPTION DEFECT`, one nested lifecycle projection omitted `.update`.

Successful source replay: commit `a61071295a8a67e6fd15dee3bae5235b3cc12c81`, run `35172578435`, artifact `10477082977` (`sha256:d9c6e8a540a839b9be70b7b37409da8e484a3241e80314de1bc2a80878c3b4d0`). Axiom-audit replay: commit `2cda465f1cd244beee4c0f747ac8ae8827a77b54`, run `35172652897`, artifact `10476888936` (`sha256:5b6ecd9d6587afdf4d65d8d9aa5a8532a87b5b810bd01805a31f03660d7a38aa`). `#print axioms` reports only `propext`, `Classical.choice`, and `Quot.sound`; no project axiom.

Nonclaims preserved: no universal exactly-once external occurrence, no rollback resistance, no cross-epoch persistence theorem beyond the exact R1 boundary.

## T12 — Target-Contract Identity Stability

R1 controlling claim: for every effect key after reservation creation, `TCID(k)` is invariant across `COMMIT_START`, `COMMIT_SUCCESS`, `COMMIT_ABORT`, `COMMIT_FAULT_UNKNOWN`, and `RECONCILE` for that same key; a target-contract change therefore requires a distinct effect admission rather than mutation of the already-reserved effect.

Lean: `EffectKernel.PhaseD.T12_targetContractIdentityStability` and corollary `EffectKernel.PhaseD.T12_tcidChange_requiresDistinctAdmission`.

Correspondence: **EXACT**.

The proof ranges over the actual `FullStep` relation for exactly the five R1-named post-reservation lifecycle transitions. In each case, TCID equality is projected from the immutable Phase C `SameEffectBinding` relation, whose `tcid` field is exact. No extensional target equivalence, cryptographic surrogate, target truth premise, or theorem conclusion is introduced as transition data.

Successful source replay: commit `66315f1cff05bdbef54ba904037ce1500eae79c2`, run `35172917110`, artifact `10477058630` (`sha256:be65ff788edb852515df1ac963815d380abc9b1fab0912e73c67c1f62372fa01`). Axiom-audit replay: commit `77cbb4a2e54236b7cb7139853070c5073e75beac`, run `35172978107`, artifact `10477223254` (`sha256:0f5cf68d3e593f3c366aa734b521a491db42cad7a7884cc982309e8bc2881b2c`). `#print axioms` reports only `propext` for both T12 declarations; no project axiom.

Nonclaims preserved: no proof that the target contract is true, sufficient, correctly implemented, collision-resistant, or faithful to any physical target.

## T13 — Target-Contract Compliance

R1 controlling claim: for every valid protected release at `ell_H(k)`, with canonical effect `a`, bound governance `G_ell`, and bound `TCID(k)`, `Req(a,G_ell) ⊆ Guarantees(TCID(k))`; failure of that inclusion makes `COMMIT_START(k)` inadmissible; target assumptions remain indexed by the same TCID at linearization.

Lean: `EffectKernel.PhaseD.T13_targetContractCompliance` and `EffectKernel.PhaseD.T13_failedInclusion_blocksCommitStart`.

Correspondence: **EXACT**.

`ell_H(k)` is the R1 T6/T7 linearization object and therefore carries the same initially-unseen/unused trace precondition already frozen for that object. T13 invokes T7 to obtain the actual immutable authorization snapshot and then unfolds only the target-guarantee conjunct of `AuthorizedAt`; it does not assume target success. T13 invokes T12 on the exact COMMIT_START full step to prove that the successor is bound to the identical TCID, and consequently that `targetAssumptions` is indexed by that same identity on both sides of linearization. The blocking corollary proves by contradiction from the frozen COMMIT_START guard that failed requirement inclusion makes the full step impossible.

Successful source replay: commit `3ba053bc1a0c4875659ea3734701359ce754c4c2`, run `35173145779`, artifact `10477736410` (`sha256:69f7c70de517a80af13845d874e3bd5ff77d70322be2564b039192e20d984b6e`). Axiom-audit replay: commit `93eb7a137d3db6935686f1dc12067ec74b7b13c9`, run `35173204853`, artifact `10477128720` (`sha256:d37f0b7d867ce7089ee8b4cc0a36f9ef2dea6477624046a50b1ef583daa4fefb`). `#print axioms` reports only `propext` for both T13 declarations; no project axiom.

Nonclaims preserved: no proof that target guarantees hold physically, no A11 discharge, no dispatcher-fidelity claim, and no T15 consequence theorem.

## T14 — Exact Effect Binding

R1 controlling claim: after PREPARE binds one canonical effect descriptor, all later lifecycle handling for that effect preserves the exact authorization-relevant descriptor; a mutation of the effect meaning or authorization binding requires a distinct fresh effect identity and admission.

Lean: `EffectKernel.PhaseD.T14_exactEffectBinding`, with bridge `EffectKernel.PhaseD.sameEffectBinding_boundDescriptor_eq`.

Correspondence: **EXACT**.

`AuthorizationBoundDescriptor` is proof-only derived material, not trusted state. It contains the frozen effect key, raw effect-AST identity, decoded canonical operation/effect-class/target/parameter/cap/payload/state-binding/adapter meaning, subject, grant/version, policy version, TCID, reserved budget, and expiry. `sameEffectBinding_boundDescriptor_eq` derives descriptor equality directly from the immutable Phase-C `SameEffectBinding` field equalities. `fullStep_preserves_binding_of_used_T14` performs exhaustive case analysis over the actual frozen transition set; a second PREPARE for the same used key is impossible from the real `preUnused` source condition. The claim-facing theorem invokes T7 only at the actual COMMIT_START snapshot and T11 for replay/fresh-admission exclusion, exactly matching the R1 DAG.

Failed-attempt preservation:
- commit `ca4f31993c0a0e62828bae86d07576ee1cd0c4a6`, run `35173864723`, job `105051142368`, artifact `10477184602` (`sha256:006739ccd4009bc94ce7f27d35f4e91d9c767debfdc617a25f529f68b7b88098`): `FORMALIZATION / TRANSCRIPTION DEFECT`, attempted use of an unavailable generated structure extensionality constant.
- commit `b56fdc5f59db0931de25b275989280d7b76171e6`, run `35182082333`, job `105076215744`, artifact `10480985954` (`sha256:94a9a5c3257b72c8802ea1dc78bbddd6187ac87660653daacfde58e5b44fcd95`): `FORMALIZATION / TRANSCRIPTION DEFECT`, dependent elimination over lifecycle projections did not close one field equation.

Successful source replay: commit `eed32b5fb2cfaf99ced90b44fe9b25e3b073b10a`, run `35182213405`, artifact `10480283010` (`sha256:d0fb377c22bb967ed7f41cc8c626b235e71ceb088d6c998c3b07ee0f23b54ed3`), proof-source SHA-256 `d65ddab1292f4dcd59ff7d3ac951e886eee458e10126645e3870fe15787b9789`. Axiom-audit replay: commit `eb64bfbd3897d1362f7581bd6648caa3c8f63a5f`, run `35182267874`, job `105076779179`, artifact `10480417905` (`sha256:b7ccdc2b1d4c1efa60d0c4b93994bb478a4a466d3a9a07727388ab4c117d6d30`). `#print axioms` reports only `propext`, `Classical.choice`, and `Quot.sound` for `T14_exactEffectBinding`; no project axiom.

Nonclaims preserved: no concrete dispatcher-fidelity theorem, no physical-world equality claim, and no receipt-as-world-truth claim.

## Current correspondence gate

- T3: `EXACT` — CLOSED.
- T5: `EXACT` — CLOSED.
- T6: `EXACT` — CLOSED.
- T7: `SEMANTICALLY EQUIVALENT` — CLOSED by `authorizedAt_snapshot_iff` plus `SnapshotBinding` preservation.
- T10: `EXACT` — CLOSED.
- T11: `EXACT` — CLOSED.
- T12: `EXACT` — CLOSED.
- T13: `EXACT` — CLOSED.
- T14: `EXACT` — CLOSED.
- No promoted theorem depends on a `PARTIAL`, `MISSING`, `CONTRADICTORY`, or `UNRESOLVED` correspondence row.
